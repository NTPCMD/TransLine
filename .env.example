# Copy to .env and fill in. Both values are Supabase *publishable* keys, safe in a client
# bundle -- but keep the real .env untracked so config changes are deliberate.
EXPO_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=<anon-key>
